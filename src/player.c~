#include "player.h"
#include "render.h"

void check_player_input(int key, Player *player)
{
    if (player->tx < 0) player->tx = 0;
    if (player->ty < 0) player->ty = 0;
    if (player->tx > COLUMNS - 1) player->tx = COLUMNS - 1;
    if (player->ty > ROWS - 1) player->ty = ROWS - 1;
}
    
