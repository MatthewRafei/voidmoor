#ifndef PLAYER_H
#define PLAYER_H

#include <stdint.h>
#include <SDL3/SDL.h>

typedef struct {
    int tx;
    int ty;
    uint32_t color;
} Player;

void check_player_input(int key, Player *player);

#endif //PLAYER_H
